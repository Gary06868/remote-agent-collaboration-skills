# Permissions

Lead can manage actors, modules, policy, tasks, announcements, reviews, requests, and compaction.

Member can only work on assigned tasks, authorized modules, acknowledgements, module logs, handoffs, and requests.

Denied actions must become structured requests.
