# Git Collaboration

Before major work:

```bash
git fetch --all --prune
git status --short --branch
git branch -vv
```

Do not use blind `git add .`. Do not force push, hard reset, clean, or rewrite shared history without explicit approval.
